#include <filesystem>
#include <fstream>
#include <iostream>
#include <sstream>

#include "fileManager.h"

using namespace std;


// Constructor implementation
FileManager::FileManager() {
    // Constructor logic (if needed)
}

// Implementation of the run method
void FileManager::run() {
    // Your run method implementation
    std::cout << "filemanager started" << std::endl;
}
